{
  "name": "stafin-frontend",
  "version": "1.0.0",
  "description": "Frontend for Stafin Homes",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build:vercel": "rm -rf vercel_build && mkdir vercel_build && cp -r dist vercel_build/ && cp -r src vercel_build/ && cp -r src/pages/* vercel_build/",
    "build": "npm run build:css && npm run build:vercel"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4"
  }
}